<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nunes Sports</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container p-0">
        <div class="header px-4 mb-4 text-center">
            <a href="/" class="logo mb-0">Nunes Sports</a>
        </div>

        <div class="rowContainer p-4">
            <div class="titleContainer d-flex align-items-center justify-content-between">
                <div>
                    <h1 class="mb-0">Produtos cadastrados</h1>
                    <p class="mb-0">Veja abaixo a lista dos produtos cadastrados</p>
                </div>

                <a href="/add" class="btn btn-outline-primary generalButton">Adicionar produto</a>
            </div>
    
            <div class="contentContainer mt-4">

                <div class="gridTable mb-4">
                    <div class="d-grid gridHeader">
                        <div>ID</div>
                        <div>Nome</div>
                        <div>Código</div>
                        <div>Descrição</div>
                        <div>Preço</div>
                        <div></div>
                    </div>

                    <% items.forEach(function(item) { %>
                        <div class="d-grid">
                            <div class="d-flex align-items-center justify-content-center" title="<%= item.id %>">
                                <span class="mobileInfo">ID:</span>
                                <span><%= item.id %></span>
                            </div>

                            <div class="d-flex align-items-center justify-content-center" title="<%= item.nome %>">
                                <span class="mobileInfo">Nome:</span>
                                <span><%= item.nome %></span>
                            </div>

                            <div class="d-flex align-items-center justify-content-center" title="<%= item.codigo %>">
                                <span class="mobileInfo">Código:</span>
                                <span><%= item.codigo %></span>
                            </div>

                            <div class="d-flex align-items-center justify-content-center" title="<%= item.descricao %>">
                                <span class="mobileInfo">Descrição:</span>
                                <span><%= item.descricao %></span>
                            </div>

                            <div class="d-flex align-items-center justify-content-center" title="<%= item.preco %>">
                                <span class="mobileInfo">Preço:</span>
                                <span><%= item.preco %></span>
                            </div>

                            <div class="d-flex align-items-center justify-content-center">
                                <a href="/edit/<%= item.id %>" class="btn btn-info mr-2">Editar</a>
                                <a href="/delete/<%= item.id %>" class="btn btn-danger">Deletar</a>
                            </div>
                        </div>
                    <% }); %>
                </div>

                <nav aria-label="Page navigation">
                    <ul class="pagination">
                      <% for(let i = 1; i <= pages; i++) { %>
                        <li class="page-item <%= i === current ? 'active' : '' %>">
                          <a class="page-link" href="/page/<%= i %>"><%= i %></a>
                        </li>
                      <% } %>
                    </ul>
                </nav>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</body>
</html>